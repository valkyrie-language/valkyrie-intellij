module A;

// missing scope means public
export { }
export super, package::a, package::b {
    nested::{`import` as export, `export` as import}
}

import package.name;
import `import` as @export;
import std::{
    import, export,
    @`import`, @`result`,
    nested.{
        `import` as @export,
        @`export` as import,
    }
}
import serde::{#Serialize, #Deserialize}

require("../test")