namespace ad.bd.cd

let a = true;


inline function package::path::range(min: int, max: int, step: int): Range {
    lambda () { };
    new lazy std::iterator::Range::<T>() {
        from: min,
        till: max,
        step: step,
        _current: min,
    }
}

trait TestTrait {

}



function test(a: A = "", `b`: `B` = null) -> ReturnType {

}

"{123}"

std::test::<T>(arg)[index];
std::test⦓T⦔(arg)⁅index⁆;


a?.like

a()


30℃ == 60℉;

for i in tes {
    +retst
}


