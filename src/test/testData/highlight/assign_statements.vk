let const a: Integer = 2;
let (mut p, ref q, lazy rec r): (int) = scope {
    pass {}.await?
};
let case Some(a) = maybe();
let std::Pair(std::Some(a), b) = maybe();

a = 2
(b, c) = 2


// 其 cps 形式是

